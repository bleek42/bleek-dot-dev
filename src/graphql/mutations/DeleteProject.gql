 mutation DeleteProject($where: ProjectWhereUniqueInput!) {
 	deleteProject(where: $where) {
 		id
		title
		active
		link
		description
		techStack
		sourceCode
		version
		createdAt
		updatedAt
		stage
		locale
 	}
 }
